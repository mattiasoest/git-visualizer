services:
  backend:
    image: ${GHCR_IMAGE:-ghcr.io/mattiasoest/git-visualizer-backend:latest}
    pull_policy: always
    ports:
      - "${SERVER_PORT:-8080}:8080"
    environment:
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
      SPRING_WEB_CORS_ALLOWED_ORIGINS: ${SPRING_WEB_CORS_ALLOWED_ORIGINS:-}
    restart: unless-stopped
